#include <stdio.h>

int main() {
    int year;
    float temp;
    double pi;
    char initial;

    printf("%zu\n", sizeof(year));
    printf("%zu\n", sizeof(temp));
    printf("%zu\n", sizeof(pi));
    printf("%zu\n", sizeof(initial));

    return 0;
}
